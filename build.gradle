subprojects {
  apply plugin: 'java'

  group = 'com.palominolabs.groovy'
  version = '1.0.0-SNAPSHOT'

  repositories {
    mavenCentral()
  }

  ext {
    depVersions = [
        slf4j: '1.7.6',
    ]
  }
}


task wrapper(type: Wrapper) {
  gradleVersion = '1.10'
}

subprojects {
  apply plugin: 'java'

  group = 'com.palominolabs.groovy'
  version = '1.0.0-SNAPSHOT'

  repositories {
    mavenCentral()
  }

}


task wrapper(type: Wrapper) {
  gradleVersion = '1.10'
}
